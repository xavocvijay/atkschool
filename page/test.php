<?php

class page_test extends Page {
	function init(){
		parent::init();

		// $m=$this->add('Model_XYZ');
		// $m_join_object = $m->join('table_x.table_field','m_field_representing_tablex');
		// $m_join_object->addField('new_field_name_from_table_x','actual_field');
		// // OR
		// $m_join_object->addField('field from table x if this name in not defined in model m');

		// $m->addConition('new_name is available in now model M');

		// $m['fewfields'] = $values;
		// $m->save();
		// // if join then table entry in joined table also
		// $m->delete();
		//Delete joined table entry also


		// ->leftJoin() 

		// Not deleted joined table if $m->delete

  


	}

	}
	